# Release 2026-06-18: OSPlus 0.2.1

**Commit:** `0dd158168232c1421c2cdd57227d2c293fae5246`
**Branch:** `main`
**Version:** `0.2.1`
**Built by:** Codex session with maintainer-provided cooked pak
**Zip size:** 39,927,521 bytes (`dist/OSPlus.zip`)
**Distributed via:** GitHub Releases
**Release URL:** https://github.com/LuizinhoF/osplus/releases/tag/v0.2.1

## What's in this build

- Linux-safe distribution zip: forward-slash paths, executable metadata for
  shell scripts and `OSPlus.exe`, and LF-only Linux scripts.
- GitHub release distribution flow with `OSPlus.zip` and `version.json` assets.
- Windows/Linux update scripts that download the latest GitHub Release and
  rerun the normal installer.

## Artifact checks

- [x] `dist/version.json` reports `0.2.1`, `LuizinhoF/osplus`, and `OSPlus.zip`.
- [x] `dist/OSPlus.zip` contains no backslash-named entries.
- [x] `install.sh`, `update.sh`, `uninstall.sh`, and `mod/sidecar/OSPlus.exe`
      appear as Unix executable entries in the zip.
- [x] `bash -n` passes for `install.sh`, `update.sh`, and `uninstall.sh`.
- [x] GitHub Release `v0.2.1` exists and is not draft/prerelease.
- [x] Release assets include `OSPlus.zip` and `version.json`.
- [x] Latest download URLs resolve with HTTP 200.

## Smoke test

- [ ] Live in-game smoke test is pending maintainer verification.

## Known issues at ship

- The package was artifact-validated and published, but not live-smoked in game
  during this Codex session.

## Notes

- The first publish attempt created a partial release before asset upload failed
  due to a publish-script URL interpolation bug. The partial release/tag were
  deleted, the script was fixed, and `v0.2.1` was recreated successfully.
