# Learnings

Every non-trivial finding from working on OSPlus lands here. The discipline is enforced by `.cursor/rules/learnings-discipline.mdc`.

## What counts as a learning

Write an entry whenever **at least one** of these is true:

- The work involved more than ~30 minutes of debugging.
- A new engine/game fact was discovered (UFunction signature, BP property, lifecycle quirk).
- A new failure mode was found (a thing that crashed, broke silently, or behaved unexpectedly).
- A new tool, harness, or workflow step was introduced or significantly changed.
- A previously-believed fact turned out to be wrong.

If you're unsure, write the entry. The cost of a 5-minute log is far less than the cost of a future agent re-deriving the answer.

## How to write one

1. Copy `_TEMPLATE.md` to `<short-slug>.md` in this directory. Use kebab-case. The slug is what someone will grep for in six months.
2. Fill in every field. Empty sections mean the learning is incomplete.
3. Update any other doc the learning invalidates (`KNOWLEDGEBASE.md`, `AGENTS.md` toolchain section, etc.) — the learning entry is *additive*, not a substitute for fixing stale docs.
4. Link from the relevant code (a comment pointing to `docs/learnings/<slug>.md`) when the lesson is about a specific code location.

## Index

Add new entries here, newest first.

| Date | Slug | Area | One-line summary |
|---|---|---|---|
| 2026-04-25 | [ue4ss-ufunction-out-param-marshaling-3-0-1](ue4ss-ufunction-out-param-marshaling-3-0-1.md) | re | **Canonical UE4SS 3.0.1 multi-out-param call shape: `inst:Fn({}, {})`** — pass one empty Lua table per declared out-param; UE4SS uses tables as the only by-reference container, writing results into `bucket.<ParamName>` for base-type params (and *into the bucket itself* for struct/TArray out-params). On 3.0.1 specifically, multiple base-type out-params collapse into the FIRST bucket (Issue #971) but you still must pass a bucket per declared param to satisfy the marshaler's argument count. The wrong shapes — `()`, `(false, nil)`, `(false, {})`, `({}, ...)` followed by primitives — all error with diagnostic-but-misleading messages; the "no table on the stack" wording is the next-shape-diagnostic, not a dead-end. End-to-end-validated against `PMIdentitySubsystem:GetAuthenticatedPlayerId` in `identity.lua` v36 (production-shipping). Refutes the call-shape conclusion in `ue4ss-outparam-marshaling-failure.md` (which is now annotated as partially superseded — its per-shape error catalog and discipline-lessons remain valid; its "not callable" verdict for the shape-class is wrong). The transferable meta-lesson: **a sweep of four "plausible" call shapes that all fail does not prove the shape-class is unreachable; check the maintainer-recommended call shape from upstream issues filtered to your shipping UE4SS version before concluding.** |
| 2026-04-25 | [ue4ss-cold-start-hook-install-pattern](ue4ss-cold-start-hook-install-pattern.md) | re | Two cold-start hook-install patterns, picked by use case: **(A) known UFunction path** → direct `RegisterHook` at module load (UFunctions live in the class table from package load — no instance, no defer, no race). **(B) discovery probe needing `cls:ForEachFunction` enumeration** → two-phase `FindFirstOf` + `NotifyOnNewObject` (instance required for enumeration; deferred install acceptable). Failure mode 1: keypress-driven install for events that fire pre-interactive (Pass 6 v1's "0 fires across 79 hooks"). Failure mode 2: copy-pasting Scenario-B install machinery into Scenario-A production code, where the `ExecuteInGameThread` defer adds ~14ms post-construction and loses the race against engine-internal post-construction probes (the first identity.lua revision shipped this and missed `GetIdentityState`). |
| 2026-04-25 | [ue4ss-multicast-delegate-add-silent-noop](ue4ss-multicast-delegate-add-silent-noop.md) | re | On this UE4SS build, `MulticastInlineDelegateProperty:Add(uobject, fname)` silently no-ops — call returns ok, `GetBindings()` stays 0 across all 6 callable methods + every bind-shape variation (cross-actor, same-actor, FName variant), `Broadcast()` succeeds at marshaling but `InvocationList` is empty. Likely root cause: vtable-offset mismatch for Omega Strikers' binary. Workaround: `RegisterHook` on the originating engine UFunction (maintainer-recommended per UE4SS Issue #455). |
| 2026-04-25 | [ue4ss-outparam-marshaling-failure](ue4ss-outparam-marshaling-failure.md) | re | **Partially superseded 2026-04-25.** Original verdict (UE4SS v3.0.1 cannot call `(Bool out, X out)` UFunctions from Lua at all) is wrong at the call-shape layer — see `ue4ss-ufunction-out-param-marshaling-3-0-1.md` for the canonical `({}, {})` shape that does work on this build. This learning's per-shape error-message catalog and its discipline-lessons (sweep multiple UFunctions, treat marshaler errors as next-shape-diagnostics) remain load-bearing. The specific `PMPlayerModel:GetCached*V1` UFunctions were never re-tested with `({}, {})` and are flagged "untested with new shape." |
| 2026-04-25 | [ue4ss-lua-multicast-delegate-binding](ue4ss-lua-multicast-delegate-binding.md) | re | `MulticastDelegateProperty:Add` takes `(UObject, FName)` — passing a Lua function is a native crash that `pcall` cannot catch. UE4SS userdata `__index` returns placeholder `userdata` for unknown keys (false-friend trap); only six method names resolve to real `function` types. Spikes touching unfamiliar native APIs need persistent crash-survivable logging with `flush()` per call. **API surface is correct but non-functional on this build** — see `ue4ss-multicast-delegate-add-silent-noop.md`. |
| 2026-04-24 | [os-runtime-data-model](os-runtime-data-model.md) | re | The local Prometheus ID is reachable via two cooperating module-load installs: `RegisterHook` on `PMIdentitySubsystem:GetIdentityState` (read trigger) + `NotifyOnNewObject` on `PMPlayerPublicProfile` (captures the FIRST profile constructed during identity bootstrap = local player). The naive "first non-empty `PlayerId`" `FindAllOf` walk fails during cold-start: cache is empty for the first ~2 `GetIdentityState` fires, then jumps to 100+ entries (friend list / matchmaking pool) where `PlayerState.PlayerNamePrivate` disambiguation also fails (it's the Windows hostname pre-bind). Construction order is the only reliable disambiguation key. `MeResponseV1` extends `PlayerPublicProfile` so one read yields the full profile struct. Per-match counters (Redirects, ShotsOnGoal, Damage, PowerUps) live on `PMPlayerMatchSummary`; the puck is internally called "Rock". |
| 2026-04-24 | [os-prometheus-api-ecosystem](os-prometheus-api-ecosystem.md) | re | Odyssey runs an undocumented "Prometheus" backend API that every OS tracker proxies; it exposes career aggregates but not per-match events or redirects — the capture gap for OSPlus. Three identifiers matter for profile binding: SteamID, Prometheus ID, display name. |
| 2026-04-23 | [cursor-dot-path-is-load-bearing](cursor-dot-path-is-load-bearing.md) | docs | Renaming `.cursor/` to `cursor/` silently disables Cursor's rule / skill / hook / MCP auto-attachment — the leading dot is the hook point, not cosmetic. Control context via per-rule frontmatter, never by relocating the folder. |
| 2026-04-23 | [lifecycle-design-back-edges-and-confidence-tiers](lifecycle-design-back-edges-and-confidence-tiers.md) | docs | Linear-with-binary-gates lifecycle hides Build-stage walls as "iteration"; design with first-class back-edges + High/Med/Low confidence tiers + named assumption lists, and split feasibility/RE out of `feature-design` into a dedicated `discover` skill. |
| 2026-04-23 | [product-architecture-coupling-via-premature-locks](product-architecture-coupling-via-premature-locks.md) | docs | Collapsing product definition and architectural deliberation into one "v1 locks" doc causes architectural choices to acquire product authority they don't deserve; split into `docs/product.md` + `docs/decisions/` (ADR-based). |
| 2026-04-21 | [lua-vararg-in-pcall-closure](lua-vararg-in-pcall-closure.md) | mod | `...` cannot cross from an outer vararg function into a nested closure inside `pcall` — it's a parse-time syntax error that crashes module load; capture varargs into locals first. |
| 2026-04-21 | [native-reaction-showemoticon-pmemoticondata](native-reaction-showemoticon-pmemoticondata.md) | re | Native OS reaction stack is data-typed: `PMEmoticonData` + `ShowEmoticon` is the safer probe than `PMEmoteData` (which bakes in striker-specific animations); `WBP_ReactionModal_C` is a live owner of the render call. |
| 2026-04-20 | [playernameprivate-machine-name-out-of-match](playernameprivate-machine-name-out-of-match.md) | mod | `PlayerState.PlayerNamePrivate` can also be the local Windows machine name out of match; reject account-ID, machine-name, and synthetic shapes explicitly across mod/sidecar/relay before persisting as a display name. |
| 2026-04-20 | [sidecar-cache-desired-state-before-ws-open](sidecar-cache-desired-state-before-ws-open.md) | sidecar | Cache desired state (room, match, identity) **before** transport readiness checks; reconnect path replays cached state instead of waiting for upstream re-emits. |
| 2026-04-19 | [playernameprivate-transient-account-id](playernameprivate-transient-account-id.md) | mod | `PlayerState.PlayerNamePrivate` can be transiently account-ID shaped; defer relay join until a friendly name is resolved/cached. |
| 2026-04-19 | [relay-room-code-regex-vs-derived-codes](relay-room-code-regex-vs-derived-codes.md) | relay | Relay regex `/^[A-Z2-9]{4}$/` rejected every mod-derived 10-char `<seed>T<team>` code; local rendering hid the failure. |
| 2026-04-19 | [sidecar-ws-keepalive](sidecar-ws-keepalive.md) | sidecar | Caddy reverse_proxy holds a zombie client WS open after the upstream restarts; sidecar must heartbeat to detect it. |
| 2026-04-18 | [ue-richtextblock-named-rows](ue-richtextblock-named-rows.md) | ue-editor | Stock UE 5.1 `URichTextBlock` has no `<color value=...>` tag — only DataTable row names work as tags. |
| 2026-04-18 | [chat-presence](chat-presence.md) | relay | v16 presence design: Lua passes `username` on join; relay broadcasts `members` as `\n`-joined string (json.lua is flat-only). |
| 2026-04-04 | [chat-match-detection-via-seed](chat-match-detection-via-seed.md) | mod | Match detection switched from `Pawn` presence to server-replicated `CurrentMatchSeed`. |
| 2026-04-04 | [oci-relay-deploy-gotchas](oci-relay-deploy-gotchas.md) | ops | Five-failure-mode gauntlet: firewall layers, CRLF, DNS, log perms, JIT vs `MemoryDenyWriteExecute`. |
| 2026-04-04 | [ue-cook-additional-asset-dirs](ue-cook-additional-asset-dirs.md) | ue-editor | "Cook Content for Windows" needs `/Game/Mods/OSPlus` in *Additional Asset Directories to Cook*. |
